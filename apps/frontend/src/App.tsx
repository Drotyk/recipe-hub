export default function App() {
  return (
    <main className="app-shell">
      <section className="hero">
        <span className="eyebrow">Algoritm Lab</span>
        <h1>Frontend workspace is ready.</h1>
        <p>
          This Vite + React app is connected to the pnpm workspace and ready for
          feature work.
        </p>
      </section>
    </main>
  );
}
